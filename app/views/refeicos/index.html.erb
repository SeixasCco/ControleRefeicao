<p class="text-box" style="color: green"><%= notice %></p>

<h1>Refeições</h1>

<div class="text-box" id="refeicos">
  <% @refeicos.each do |refeico| %>
    <%= render refeico %>
    <p>
      <%= link_to "Mostrar essa refeição", refeico %>
    </p>
  <% end %>
</div>

<%= link_to "Nova refeição", new_refeico_path %>
