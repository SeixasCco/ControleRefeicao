<%= form_with(model: refeico) do |form| %>
  <% if refeico.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(refeico.errors.count, "error") %> prohibited this refeico from being saved:</h2>

      <ul>
        <% refeico.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :refeicao, style: "display: block" %>
    <%= form.text_field :refeicao %>
  </div>

  <div>
    <%= form.label :data, style: "display: block" %>
    <%= form.date_field :data %>
  </div>

  <div>
  <%= form.submit "Criar refeição" %>
  </div>
<% end %>
